#include "monty.h"

/**
 * op_stack - The function prints tbe top of the stack
 * @first: The first node;
 * @increment: The line count
 * Return: void
 */

void op_stack(stack_t **first, unsigned int increment)
{
	(void)first;
	(void)increment;
	buff.lifo_flag = 0;
}
